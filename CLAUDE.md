# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The project guide lives in `AGENTS.md` so that every coding agent reads the same
one. It is imported below — read it.

@AGENTS.md
